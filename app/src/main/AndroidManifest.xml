<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a1000_7">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.10007">
        <activity android:name=".chat"></activity>
        <activity android:name=".job" />
        <activity android:name=".list" />
        <activity android:name=".musician" />
        <activity android:name=".wine" />
        <activity android:name=".film" />
        <activity android:name=".cinema" />
        <activity android:name=".choices" />
        <activity android:name=".stunt" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>